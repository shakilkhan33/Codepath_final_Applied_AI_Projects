---
name: "Implement PawPal Feature"
description: "Implement a new PawPal feature with code changes, tests, and verification steps."
agent: "PawPal Dev"
argument-hint: "Feature request, acceptance criteria, and constraints"
---
Implement this PawPal feature request:

${input:Feature request}

Requirements:
- Keep domain rules in `pawpal_system.py`.
- Update Streamlit flow in `app.py` only if needed for UX.
- Add or update tests in `test_pawpal.py`.
- Run relevant tests and report results.
- Summarize changed files and rationale.
